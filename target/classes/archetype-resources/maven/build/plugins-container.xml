<!-- container build plugins -->
<!--
<plugin>
  <groupId>com.google.cloud.tools</groupId>
  <artifactId>jib-maven-plugin</artifactId>
  <version>${jib.version}</version>
  <configuration>
    <to>
      <image>myrepo/${project.artifactId}:${project.version}</image>
    </to>
  </configuration>
</plugin>
-->